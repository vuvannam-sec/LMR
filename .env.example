# Docker Compose development defaults.
# These values are intentionally local-only and must not be reused for exposed deployments.
MYSQL_DATABASE=lms_mvp
MYSQL_USER=lms
MYSQL_PASSWORD=local-lms-only
MYSQL_ROOT_PASSWORD=local-root-only
MYSQL_PORT=3307
PMA_PORT=8081
